# Adapters

Adapter entry points translate CLI, dashboard, and other outside callers into application use cases. They should not own task lifecycle semantics or bypass repository/operation contracts.
